---
import { RichText } from "../../../lib/types/interfaces.ts"

export interface Props {
  richText: RichText
}

const { richText } = Astro.props
---

{
  /* prettier-ignore */
  richText.Annotation.Bold ? (
    <b><slot /></b>
  ) : (
    <slot />
  )
}
